import { AddActiveClassDirective } from './add-active-class.directive';

describe('AddActiveClassDirective', () => {
  it('should create an instance', () => {
    const directive = new AddActiveClassDirective();
    expect(directive).toBeTruthy();
  });
});
